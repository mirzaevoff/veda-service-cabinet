# Veda Service — гайд по стилю и дизайну (веб-кабинет)

Кратко: фирменный стиль Veda Vector (vedavector.uz) — белый фон, много воздуха,
глубокий красный как единственный акцент, шрифт Unbounded для бренда, Inter для
всего остального. Никаких эмодзи в UI — только SVG-иконки (lucide).

Единый гайд с мобильным приложением (`com.vedavector.mobile/STYLEGUIDE.md`);
здесь — адаптация под веб. Источник токенов: `src/app/globals.css`.

## Цвета

### Светлая тема (основная)

| Токен гайда | HEX | shadcn-переменная | Использование |
|---|---|---|---|
| `accent` | `#A21500` | `--primary`, `--ring` | Фирменный красный: кнопки, ссылки, активные состояния, логотип |
| `accentBright` | `#E61D00` | `--accent-bright` | Hover/pressed для accent |
| `accentLight` | `#FFE8E5` | `--accent-light`, `--accent` | Розовые плашки: фон выбранных элементов, бейджи, иконки-подложки |
| `background` | `#FFFFFF` | `--background`, `--card` | Фон страницы, карточки |
| `surfaceSecondary` | `#F6F6F6` | `--secondary`, `--muted` | Заливка полей/ячеек, вторичные кнопки |
| `border` | `#D2D2D2` | `--border`, `--input` | Рамки неактивных полей |
| `textPrimary` | `#111111` | `--foreground` | Основной текст, заголовки |
| `textSecondary` | `#747474` | `--muted-foreground` | Подзаголовки, подписи |
| `textTertiary` | `#A3A3A3` | — | Плейсхолдеры, самый тихий текст |
| `error` | `#E61D00` | `--destructive` | Ошибки (фон — `#FFE8E5`) |
| `success` | `#10B981` | `--success` | Успех (фон — `--success-light`) |
| `warning` | `#F59E0B` | `--warning` | Предупреждения (фон — `--warning-light`) |

### Тёмная тема

| Токен | HEX |
|---|---|
| `accent` / `accentBright` | `#FF5C42` / `#FF3D1F` |
| `accentLight` | `#3A130B` |
| `background` / `surface` | `#121212` / `#1C1C1C` |
| `surfaceSecondary` | `#262626` |
| `border` | `#3A3A3A` |
| `textPrimary` / `textSecondary` / `textTertiary` | `#F3F3F3` / `#A3A3A3` / `#747474` |

Правило: красный — единственный цветовой акцент. Не вводить синие/зелёные акценты,
success/warning — только для статусов.

## Типографика

Оба шрифта подключены через `next/font/google` (кириллица + латиница).

- **Unbounded** (700/600, класс `font-brand`) — ТОЛЬКО бренд: логотип
  «Veda Service», крупные вордмарки. Не использовать для обычных заголовков.
- **Inter** (`font-sans`, дефолт) — всё остальное.

| Стиль | Шрифт | Размер/интерлиньяж | Применение |
|---|---|---|---|
| display | Unbounded 700 | 36–60/1.2 | Вордмарк на хиро |
| brand small | Unbounded 600 | 16/22 | Маленький логотип в шапке |
| h1 | Inter 700 | 32/40 | Главный заголовок страницы (умеренно) |
| h2 | Inter 700 | 24/32 | Заголовки секций |
| заголовок формы | Inter 600 | 22/28 | «Введите код из SMS» |
| body | Inter 400/500/600 | 16/24 | Текст, значения, кнопки (600) |
| caption | Inter 400/500 | 14/20 | Подписи, подсказки |
| small | Inter 400 | 12/16 | Версии, самое мелкое |
| legal | Inter 400/500 | 11/15 | Юридические тексты |

Тон текстов: короткие фразы, на «вы» (с маленькой), без канцелярита, информативно:
не «Ошибка!», а «Код не подходит или истёк. Проверьте цифры из SMS».

## Сетка и скругления

- Отступы (шкала): 4 / 8 / 12 / 16 / 20 / 24 / 32 / 48
- Скругления: 8 (sm) / 12 (md — поля ввода) / 16 (lg — карточки, бейджи-иконки) /
  pill (кнопки, чипы). В токенах `--radius: 1rem` → `rounded-lg` = 16.

## Компоненты

### Кнопка primary
- Полный pill-радиус, фон `primary`, текст белый Inter 600
- Hover/pressed → `accent-bright`; disabled → приглушённый; загрузка — спиннер

### Кнопка outline
- Pill, прозрачный фон, рамка `primary`, текст/иконка `primary`
- Hover → фон `accent-light`

### Поле ввода
- Радиус 12 (`rounded-md`), фон белый, рамка 1.5 `border`
- Фокус → рамка `primary`; ошибка → рамка `destructive` + текст 12px под полем
- Лейбл 14/500 `muted-foreground` над полем

### Ссылки
- Цвет `primary`, подчёркивание; hover → `accent-bright`

### Бейджи/чипы
- Pill, фон `accent-light` + тёмный текст (акцентные) или `secondary` +
  `muted-foreground` (нейтральные: «Скоро»)

### Иконки
- Только SVG (lucide), stroke 1.75–2, без эмодзи
- Иконка-подложка: 56×56, радиус 16, фон `accent-light`, иконка `primary` 26px

### Модалки/диалоги
- Затемнение `rgba(0,0,0,0.4)`, скругление 16, высота по содержимому

## Анимации

- Появление контента: fade + подъём на 16px, 450 мс, ease-out (cubic)
- Ввод/выбор: короткий «поп» масштабом 1 → 1.12 → 1, ~200 мс
- Ошибка: горизонтальная тряска ±12px, ~350 мс
- Переходы страниц: кросс-фейд 450 мс
- Ничего дольше 500 мс; анимации подтверждают действие, а не развлекают

## Справка

- Сайт-референс: https://vedavector.uz
- Шрифты: https://fonts.google.com/specimen/Unbounded, https://fonts.google.com/specimen/Inter
- Иконки: https://lucide.dev
- Мобильный гайд-первоисточник: `com.vedavector.mobile/STYLEGUIDE.md`
